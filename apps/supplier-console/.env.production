# Production build (hatpro.archon.technology). Loaded automatically by `vite build`.
# Same-origin path, reverse-proxied by nginx to the resort keymaster API.
VITE_RESORT_API_URL=/resort-api

# Resolver: in-browser DID resolution uses the public gatekeeper.
VITE_GATEKEEPER_URL=https://archon.technology
